"""觀眾可以觸發的功能，一個模組一個功能。

**Google Sheets「指令集」的 function 型指令只會指向這個套件底下的模組。**
那張表是頻道主手動維護的，改動它的成本比改程式高（要一格一格改），
所以這個約定要靠測試守著（見 tests/test_dispatcher.py 的相容路徑測試）。

功能自己的狀態就住在自己的模組裡——遊戲盤面、VIP 設定、當日餐點快取都是。
別的地方（bot、排程）要驅動功能是正常的：排程會開遊戲、啟動時會掃過期 VIP。
只有「不只功能自己在用」的東西才抽出去，例如試算表載入在 sheet_config。
"""
